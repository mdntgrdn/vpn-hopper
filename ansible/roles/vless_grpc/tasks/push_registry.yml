---
- name: vless-grpc config directory
  ansible.builtin.file:
    path: "{{ vless_grpc_dir }}"
    state: directory
    mode: "0755"

- name: Push vless_clients registry to wrapper host
  ansible.builtin.copy:
    src: "{{ awg_work_group_vars }}/vless_clients.yml"
    dest: "{{ vless_grpc_vless_clients }}"
    mode: "0600"

- name: Persist rendered config.json on wrapper host
  ansible.builtin.copy:
    src: "{{ awg_work_vless_grpc_configs }}/{{ inventory_hostname }}.json"
    dest: "{{ vless_grpc_config_path }}"
    mode: "0644"
