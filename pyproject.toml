[tool.poetry]
package-mode = true
packages = [{include = "scripts", from = "."}]

[project]
name = "awg-ansible-controller"
version = "0.1.0"
description = ""
authors = [
    {name = "Pavel Merkulov",email = "merkulov0pavel@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ansible-core (>=2.20.5,<3.0.0)",
    "jinja2 (>=3.0,<4.0)",
    "pyyaml (>=6.0,<7.0)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
